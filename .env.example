# Database (Prisma) — Vercel/Neon: ใช้ pooled URL (มี -pooler) + ?sslmode=require
# ตัวอย่าง: postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=''

# บังคับบน Vercel production — ใช้ sign session cookie (openssl rand -base64 32)
SESSION_SECRET=''

CLICKUP_API_TOKEN=""
CLICKUP_TEAM_ID=""
